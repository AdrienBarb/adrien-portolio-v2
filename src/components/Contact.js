import React from 'react'

const Contact = (props) => {
    return (
        <div>
            <div>{props.title}</div>
            <div>{props.info}</div>
        </div>
    )
}

export default Contact
